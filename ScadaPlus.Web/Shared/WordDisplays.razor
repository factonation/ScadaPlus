@if (Words is not null)
{
    @for (int i = 0; i < Words.Length; i++)
    {
        int index = i;
        <MudChip Color="Color.Info">@Words[index]</MudChip>
    }
}

@code {
    [Parameter]
    public short[]? Words { get; set; }
}
